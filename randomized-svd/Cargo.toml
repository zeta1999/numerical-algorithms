[package]
name = "randomized-svd"
version = "0.1.0"
edition = "2021"
description = "Randomized SVD algorithm from scratch in Rust"
license = "MIT"

[dependencies]
ndarray = "0.15"
rand = "0.8"
rand_distr = "0.4"

[dev-dependencies]
